apiVersion: v1
kind: Service
metadata:
  labels:
    app: scaletestapp
  name: scaletestapp
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 31000
  selector:
    app: scaletestapp
  type: NodePort